@model IEnumerable<WebMathTraining.Models.TestGroup>

@{
    ViewData["Title"] = "Membmer Teams";
}

<h2>Registered Member Teams</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.LastUpdatedLocal)
            </th>
            @*<th>
                @Html.DisplayNameFor(model => model.TeamHeadId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MembersInfo)
            </th>*@
            <th>
                @Html.DisplayNameFor(model => model.EnrolledSessionsInfo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastUpdated)
            </td>
            @*<td>
                @Html.DisplayFor(modelItem => item.TeamHeadId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MembersInfo)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.EnrolledSessionsInfo)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |*@
                <a asp-action="Details" asp-route-id="@item.Id">Latest Ranking</a> 
                @*<a asp-action="Delete" asp-route-id="@item.Id">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
